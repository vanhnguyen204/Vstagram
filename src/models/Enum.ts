enum ImageEditorScreen {
  EDITOR = 'EDITOR',
  CREATE_NEW = 'CREATE_NEW',
}

enum MediaType {
  POSTS = 'POSTS',
  REELS = 'REELS',
  STORY = 'STORY',
}
enum StoryType {
  PHOTO = 'PHOTO',
  VIDEO = 'VIDEO',
}
export {ImageEditorScreen, MediaType, StoryType};
